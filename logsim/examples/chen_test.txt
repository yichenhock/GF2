devices( 
      a, q, b are 
AND;
      c is XOR;
      sw, sw1 is SWITCH;      
      clk is CLOCK;

)

initialise(
    sw, sw1 is HIGH; 
    clk cycle 100;
    a, q has 4 inputs;
    c has 3 inputs;
)